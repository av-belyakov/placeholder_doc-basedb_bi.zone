version: "3.4"

services:
  app:
    container_name: placeholder_doc-basedb_bi.zone.prod
    environment:
      - GO_PHDOCBASEDBBZ_DBWLOGPASSWD=$PHDOCBASEDBBZ_DBWLOGPASSWD
      - GO_PHDOCBASEDBBZ_DBSTORAGEPASSWD=$PROD_PHDOCBASEDBBZ_DBSTORAGEPASSWD
    image: gitlab.cloud.gcm:5050/a.belyakov/placeholder-doc-basedb-bi-zone:latest
    restart: unless-stopped
    #пробрасываем user ID для корректной записи в логов
    user: root
    command: chmod 777 /opt/placeholder_doc-basedb_bi.zone/logs && \
             chown 1500:1500 /opt/placeholder_doc-basedb_bi.zone/logs
    volumes:
      - /var/log/placeholder_doc-basedb_bi.zone.prod:/opt/placeholder_doc-basedb_bi.zone/logs:rw
      - ./config:/opt/placeholder_doc-basedb_bi.zone/config:rw
      #выполняем для корректного отображения даты с учетом временной зоны
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    network_mode: "host"